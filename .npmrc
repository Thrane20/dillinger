# Root .npmrc
# Provides a dummy auth token for the local Verdaccio registry so pnpm
# does not reject publish commands with "not logged in".
# Verdaccio accepts this for dillinger-gaming because publish is set to $all
# (no real authentication required).
//localhost:4873/:_authToken=local-dev-anonymous
