# Build Notes

1. Currently hardcoding Docker BuildX to 0.16.2 which is at "https://github.com/docker/buildx/releases/download/v0.16.0/buildx-v0.16.0.linux-amd64"

This will probably suffice for many years to come, but if the build container fails to rebuild on this, better find a valid download URL with the latest.

