import { formatLastPlayed, formatPlayTime } from '@dillinger/shared';

/**
 * Re-export for backwards compatibility
 * @deprecated Import directly from @dillinger/shared instead
 */
export { formatLastPlayed, formatPlayTime };
